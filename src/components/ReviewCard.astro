---
interface Props {
  name: string;
  location: string;
  dates: string;
  duration: string;
  quote: string;
  highlight?: boolean;
}

const { name, location, dates, duration, quote, highlight = false } = Astro.props;
---

<figure
  class={`flex flex-col justify-between rounded-2xl border p-6 ${
    highlight
      ? "border-primary-950 bg-primary-950 text-primary-50"
      : "border-primary-200 bg-white text-primary-900"
  }`}
>
  <blockquote class="text-lg leading-relaxed">&laquo;{quote}&raquo;</blockquote>
  <figcaption class="mt-6">
    <p class="font-medium">{name}</p>
    <p class={highlight ? "text-primary-300" : "text-primary-500"}>
      {location} &middot; {dates}
    </p>
    <p
      class={`mt-2 inline-block rounded-full px-3 py-1 text-xs font-medium ${
        highlight ? "bg-primary-50 text-primary-950" : "bg-primary-100 text-primary-700"
      }`}
    >
      {duration}
    </p>
  </figcaption>
</figure>
